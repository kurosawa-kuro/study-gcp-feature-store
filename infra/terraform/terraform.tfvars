project_id = "mlops-dev-a"
region     = "asia-northeast1"

# image は Makefile が -var=image=... で渡す (build-push 後)。
# 単体で apply するときは下記を埋める:
# image = "asia-northeast1-docker.pkg.dev/mlops-dev-a/feature-store/feature-store-job:latest"
